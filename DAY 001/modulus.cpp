#include<iostream>
using namespace std;
int main(){
    int x,y;
    cout<<"enter a no. : ";
    cin>>x;
    cout<<"enter a no. : ";
    cin>>y;
    cout<<x%y;
    // a % b = a (if a<b)
    // a % a = 0
    // a % (-b) = a % b
    // (-a) % b = -[a % b]
    }